web:
  image: asermax/plus
  links:
    - db
  ports:
    - "80:8000"
  volumes:
    - plus_dependencies:/opt/app/node_modules
  environment:
    NODE_ENV: production
    MONGO_URL: mongodb://db:27017/mern-starter
db:
  image: mongo:latest
  ports:
    - "27017:27017"

